source 'https://rubygems.org'

# Specify your gem's dependencies in hcp.gemspec
gemspec

gem 'company', path: '../company' # the vocabulary is unreleased, so the sibling stands in
gem 'debug', require: 'debug/prelude' # bin/console stops at a breakpoint without it
gem 'irb' # bin/console has no REPL to start without it
